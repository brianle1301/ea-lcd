import { Typography } from '@mui/joy';

import Body from '../components/Body';

const FirstSitting = function () {
    return (
        <Body>
            <Typography level="h1">Hey! Want to Move?</Typography>
        </Body>
    );
};

export default FirstSitting;
