import { Box, Typography } from '@mui/joy';

const DoItTwiceSit = function () {
    return (
        <Typography level="h1" display="flex" alignItems="center">
            End with the goal in mind "Do it twice" Sit
            <Box component="img" mx={2} height={40} src="/assets/DoittwiceSit.png" />
        </Typography>
    );
};

export default DoItTwiceSit;
