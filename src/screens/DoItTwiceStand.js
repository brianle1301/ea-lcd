import { Box, Typography } from '@mui/joy';

const DoItTwiceStand = function () {
    return (
        <Typography level="h1" display="flex" alignItems="center">
            End with the goal in mind "Do it twice" Stand
            <Box component="img" mx={2} height={40} src="/assets/DoittwiceStand.png" />
        </Typography>
    );
};

export default DoItTwiceStand;
