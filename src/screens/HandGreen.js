import { Typography } from '@mui/joy';

const HandGreen = function () {
    return (
        <Typography level="h1">These turn green when your hands are on the armchair​</Typography>
    );
};

export default HandGreen;
